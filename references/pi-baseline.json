{
  "schema_version": 3,
  "verified_ref": "references/pi-baseline.json",
  "product": {
    "name": "Pi Coding Agent",
    "site": "https://pi.dev/",
    "documentation": "https://pi.dev/docs/latest",
    "repository": "https://github.com/earendil-works/pi"
  },
  "package": {
    "name": "@earendil-works/pi-coding-agent",
    "version": "0.85.0",
    "registry": "https://registry.npmjs.org/@earendil-works%2fpi-coding-agent",
    "tarball": "https://registry.npmjs.org/@earendil-works/pi-coding-agent/-/pi-coding-agent-0.85.0.tgz",
    "integrity": "sha512-INxVkLAVfAMju5MojJpmyu/0bMP+r+ffZuS7UqVv32E2JwHBRbcHfELDfmFNvapEbgYfKN2r9OYO1p3TqDBR+g==",
    "shasum": "063fab0fe6d5f6654e09b479e95dd5e2359824c4",
    "node": ">=22.19.0",
    "repository_directory": "packages/coding-agent",
    "note": "**This block describes the npm distribution, which this provider no longer installs.** It is kept because it is a true and useful record of the product -- `bin.pi`, `engines.node`, the registry tarball and its integrity -- and because the version it names is the one the standalone release carries. `node: >=22.19.0` is a requirement of *that* distribution: the artifacts installed here carry their own runtime, and the whole lifecycle was measured 2026-08-31 with `node` absent from `PATH`."
  },
  "cli_identity": {
    "command": "pi",
    "version_probe": {
      "argv": [
        "pi",
        "--version"
      ],
      "expected_stdout": "0.85.0",
      "package_version_source": "package.version",
      "independent_package_identity_required": true
    },
    "install_docs": "https://pi.dev/docs/latest",
    "install_script": "https://pi.dev/install.sh",
    "note": "**No `bin` here, and its absence is deliberate.** It held `dist/cli.js` while the artifact table said `package/dist/bundle/cli.js`, and the version probe expected `0.0.0` beside a pin of `0.84.4` -- one fact kept twice and allowed to disagree, in both fields. Measured 2026-08-31 from the pinned bytes: the npm package declares `bin.pi = dist/bundle/cli.js` and prints `0.84.4`.\n\n**And then the entry point stopped being one value.** This provider now installs the vendor's standalone release rather than the npm package, and the executable inside it is `pi/pi` in the four gzip-tars and `pi.exe` at the root of the two zips. A single `bin` string cannot hold both, and a copy that can only be right for some platforms is worse than no copy. The artifact table's `member` is the owner of that fact, per platform, and the artifact-table transcription check refuses this block if it grows a second copy that disagrees.\n\n`expected_stdout` stays, because a version the product prints is not derivable from a member and is the one thing a run can check.",
    "entry_point_source": "software_artifacts.platforms[*].member"
  },
  "launch_scope": {
    "target_role": "configuration-runtime-home",
    "default_workspace_source": "caller-cwd-captured-once",
    "manager_workspace_option": "--workspace <absolute-existing-dir>",
    "explicit_workspace_requirements": [
      "absolute",
      "existing-directory",
      "final-component-not-symlink",
      "accessible"
    ],
    "child_cwd": "resolved-workspace",
    "native_workspace_argument_supported": false,
    "native_workspace_argument": null,
    "official_cli_grammar": {
      "package": "@earendil-works/pi-coding-agent@0.84.1",
      "parser": "dist/cli/args.js",
      "documentation": "https://pi.dev/docs/latest/usage",
      "workspace_project_cwd_flags": []
    }
  },
  "manager_installation": {
    "tool": "npm",
    "argv": [
      "install",
      "--global-style",
      "--ignore-scripts",
      "--no-audit",
      "--no-fund",
      "--package-lock=false",
      "--prefix",
      "<stage>/install",
      "<verified-tarball>"
    ],
    "trust": false,
    "consumer_lifecycle_scripts": {
      "preinstall": null,
      "install": null,
      "postinstall": null,
      "prepublishOnly": "npm run clean && npm run build && npm run shrinkwrap"
    },
    "staging_environment": {
      "HOME": "<stage>/home",
      "npm_config_cache": "<stage>/cache",
      "npm_config_ignore_scripts": "true",
      "npm_config_userconfig": "<stage>/npmrc",
      "XDG_CONFIG_HOME": "<stage>/xdg-config",
      "TMPDIR": "<stage>/tmp"
    },
    "metadata_argv": [
      "view",
      "@earendil-works/pi-coding-agent@0.84.1",
      "dist",
      "--json"
    ],
    "pack_argv": [
      "pack",
      "--json",
      "--ignore-scripts",
      "--pack-destination",
      "<stage>/tarballs",
      "@earendil-works/pi-coding-agent@0.84.1"
    ],
    "byte_verification": {
      "metadata_integrity": "sha512-ncAqFrG+iybuPGOhMiZoEHkEzTpJgz3guYD32pD+M7ucc0WeHmauP6wa7qwP8V/KWvsZDVNa5XGsdZ7fkC7w7A==",
      "metadata_shasum": "e098cada629fdeeb9df6e77c6d480d43e1b2c553",
      "tarball_integrity": "sha512-ncAqFrG+iybuPGOhMiZoEHkEzTpJgz3guYD32pD+M7ucc0WeHmauP6wa7qwP8V/KWvsZDVNa5XGsdZ7fkC7w7A==",
      "tarball_shasum": "e098cada629fdeeb9df6e77c6d480d43e1b2c553",
      "verified_before_extract": true
    },
    "local_extraction": {
      "scripts_disabled": true,
      "verified_before_extract": true
    },
    "local_install_argv": [
      "install",
      "--global-style",
      "--ignore-scripts",
      "--no-audit",
      "--no-fund",
      "--package-lock=false",
      "--prefix",
      "<stage>/install",
      "<verified-tarball>"
    ]
  },
  "configuration": {
    "global_settings": "~/.pi/agent/settings.json",
    "project_settings": ".pi/settings.json",
    "trust_file": "~/.pi/agent/trust.json",
    "environment_override": "PI_CODING_AGENT_DIR",
    "session_environment_override": "PI_CODING_AGENT_SESSION_DIR",
    "package_environment_override": "PI_PACKAGE_DIR",
    "offline_environment_override": "PI_OFFLINE",
    "skip_version_check_environment_override": "PI_SKIP_VERSION_CHECK",
    "telemetry_environment_override": "PI_TELEMETRY",
    "documentation": "https://pi.dev/docs/latest/settings"
  },
  "native_capability_surfaces": {
    "settings": "https://pi.dev/docs/latest/settings",
    "skills": {
      "settings_key": "skills",
      "enable_commands_key": "enableSkillCommands",
      "global_locations": [
        "~/.pi/agent/skills",
        "~/.agents/skills"
      ],
      "project_locations": [
        ".pi/skills",
        ".agents/skills"
      ],
      "cli_flags": [
        "--skill",
        "--no-skills"
      ]
    },
    "packages": {
      "settings_key": "packages",
      "resource_manifest_key": "pi.skills",
      "commands": [
        "pi install",
        "pi remove",
        "pi list",
        "pi update"
      ],
      "documentation": "https://pi.dev/docs/latest/packages"
    },
    "extensions": {
      "settings_key": "extensions",
      "package_manifest_key": "pi.extensions",
      "documentation": "https://pi.dev/docs/latest"
    }
  },
  "permission_model": {
    "built_in_filesystem_process_network_restrictions": false,
    "permission_popups": false,
    "plan_mode": false,
    "project_trust_controls": [
      "defaultProjectTrust",
      "--approve",
      "--no-approve"
    ],
    "source": "https://github.com/earendil-works/pi"
  },
  "manager_projection": {
    "builder_default_on": true,
    "builder_surfaces": [
      "settings.skills",
      "settings.packages",
      "package.pi.skills",
      "agent.AGENTS.md"
    ],
    "external_marketplace_published": null,
    "plugin_marketplace": null,
    "mcp_projection": null
  },
  "verified_at": "2026-09-04T13:31:46+00:00",
  "native_surfaces": {
    "verified_at": "2026-08-31",
    "config_home": "~/.pi/agent",
    "surfaces": [
      {
        "path": "AGENTS.md",
        "kinds": [
          "instruction"
        ],
        "shape": "file",
        "source": "https://pi.dev/docs/latest/sdk; confirmed against the product's own shipped documentation, read from the 0.84.3 package at node_modules/@earendil-works/pi-coding-agent/docs",
        "evidence": "bytes",
        "note": "**Re-asked at 0.84.4 on 2026-08-31.** The pinned npm package was fetched and its `integrity` checked before a byte was read; the row still stands (AGENTS.md 57). Searched with three controls -- `nddev_invented_reference_line`, `~/.nddev-not-a-home/skills/` and `NDDEV_INVENTED_SURFACE` -- each zero in the same bytes, so the search discriminates."
      },
      {
        "path": "APPEND_SYSTEM.md",
        "kinds": [],
        "shape": "file",
        "source": "https://github.com/earendil-works/pi/releases/tag/v0.84.4",
        "evidence": "bytes",
        "note": "Named by the product's own shipped documentation at the current pin: `docs/usage.md` gives *\"`~/.pi/agent/SYSTEM.md` globally\"* beside `.pi/SYSTEM.md` for a project, and *\"Append to the default prompt without replacing it with `APPEND_SYSTEM.md` in either location\"*. `docs/security.md` lists the project pair among the resources project trust gates. Read 2026-08-31 out of the 0.84.4 artifact, whose digest was checked against this baseline's artifact table first; the file is byte-identical in the standalone distribution this provider installs and in the npm package.\n\n**Neither was owned nor declined until now -- absent from the record entirely**, which is the one state a surface must not be in: a declined row is a decision and an owned row is a promise, and nothing is neither.\n\nOwned as **custody**. No component kind can reach it: `instruction` already routes to `AGENTS.md` and the protocol has one destination per kind. Before `custody_namespaces` that would have made ownership purely destructive -- every posture ships nothing here, so selecting one emptied a person's system prompt. Now a backup captures it, the identity hashes it, `remove` returns the target to unmanaged, and no posture switch touches it."
      },
      {
        "path": "SYSTEM.md",
        "kinds": [],
        "shape": "file",
        "source": "https://github.com/earendil-works/pi/releases/tag/v0.84.4",
        "evidence": "bytes",
        "note": "Named by the product's own shipped documentation at the current pin: `docs/usage.md` gives *\"`~/.pi/agent/SYSTEM.md` globally\"* beside `.pi/SYSTEM.md` for a project, and *\"Append to the default prompt without replacing it with `APPEND_SYSTEM.md` in either location\"*. `docs/security.md` lists the project pair among the resources project trust gates. Read 2026-08-31 out of the 0.84.4 artifact, whose digest was checked against this baseline's artifact table first; the file is byte-identical in the standalone distribution this provider installs and in the npm package.\n\n**Neither was owned nor declined until now -- absent from the record entirely**, which is the one state a surface must not be in: a declined row is a decision and an owned row is a promise, and nothing is neither.\n\nOwned as **custody**. No component kind can reach it: `instruction` already routes to `AGENTS.md` and the protocol has one destination per kind. Before `custody_namespaces` that would have made ownership purely destructive -- every posture ships nothing here, so selecting one emptied a person's system prompt. Now a backup captures it, the identity hashes it, `remove` returns the target to unmanaged, and no posture switch touches it."
      },
      {
        "path": "extensions",
        "kinds": [
          "plugin"
        ],
        "shape": "directory",
        "source": "https://pi.dev/docs/latest/extensions; confirmed against the product's own shipped documentation, read from the 0.84.3 package at node_modules/@earendil-works/pi-coding-agent/docs",
        "evidence": "bytes",
        "note": "**Re-asked at 0.84.4 on 2026-08-31.** The pinned npm package was fetched and its `integrity` checked before a byte was read; the row still stands (`extensions` 2158). Searched with three controls -- `nddev_invented_reference_line`, `~/.nddev-not-a-home/skills/` and `NDDEV_INVENTED_SURFACE` -- each zero in the same bytes, so the search discriminates."
      },
      {
        "path": "prompts",
        "kinds": [
          "command"
        ],
        "shape": "directory",
        "source": "https://pi.dev/docs/latest/prompt-templates; confirmed against the product's own shipped documentation, read from the 0.84.3 package at node_modules/@earendil-works/pi-coding-agent/docs",
        "evidence": "bytes",
        "note": "~/.pi/agent/prompts/*.md; discovery is non-recursive\n\n**Re-asked at 0.84.4 on 2026-08-31.** The pinned npm package was fetched and its `integrity` checked before a byte was read; the row still stands (`prompt-templates` 50, `prompts` 560). Searched with three controls -- `nddev_invented_reference_line`, `~/.nddev-not-a-home/skills/` and `NDDEV_INVENTED_SURFACE` -- each zero in the same bytes, so the search discriminates."
      },
      {
        "path": "settings.json",
        "kinds": [
          "setting"
        ],
        "shape": "file",
        "source": "https://pi.dev/docs/latest/settings; confirmed against the product's own shipped documentation, read from the 0.84.3 package at node_modules/@earendil-works/pi-coding-agent/docs; named on screen by `pi config` against the 0.84.3 package",
        "evidence": "ran",
        "note": "paths in this file resolve relative to ~/.pi/agent Confirmed 2026-08-29 by running the 0.84.3 package: `pi config`, which needs no credential where a prompt does, opens on the global-resources scope and names `~/.pi/agent/settings.json` on screen as the file it manages. One caution recorded with it: that label did not change when `PI_CODING_AGENT_DIR` was pointed elsewhere, so it may be display text rather than a resolved path -- which costs this route nothing, because `~/.pi/agent` is the target this provider is handed, but would matter to anyone reading the line as proof that the home follows the variable.\n\n**Re-asked at 0.84.4 on 2026-08-31.** The pinned npm package was fetched and its `integrity` checked before a byte was read; the row still stands (`settings.json` 172). Searched with three controls -- `nddev_invented_reference_line`, `~/.nddev-not-a-home/skills/` and `NDDEV_INVENTED_SURFACE` -- each zero in the same bytes, so the search discriminates."
      },
      {
        "path": "skills",
        "kinds": [
          "skill"
        ],
        "shape": "directory",
        "source": "https://pi.dev/docs/latest/skills; confirmed against the product's own shipped documentation, read from the 0.84.3 package at node_modules/@earendil-works/pi-coding-agent/docs",
        "evidence": "bytes",
        "note": "**Re-asked at 0.84.4 on 2026-08-31.** The pinned npm package was fetched and its `integrity` checked before a byte was read; the row still stands (`skills` 1026). Searched with three controls -- `nddev_invented_reference_line`, `~/.nddev-not-a-home/skills/` and `NDDEV_INVENTED_SURFACE` -- each zero in the same bytes, so the search discriminates."
      },
      {
        "path": "themes",
        "kinds": [],
        "shape": "directory",
        "source": "https://pi.dev/docs/latest/themes; confirmed against the product's own shipped documentation, read from the 0.84.3 package at node_modules/@earendil-works/pi-coding-agent/docs",
        "evidence": "bytes",
        "note": "`~/.pi/agent/themes/*.json`, a JSON file per theme defining all 51 required colour tokens; `settings.json`'s `theme` names which one is active. Owned so a setup can carry a theme; the key that selects it is a setting, so no component kind routes here\n\n**Re-asked at 0.84.4 on 2026-08-31.** The pinned npm package was fetched and its `integrity` checked before a byte was read; the row still stands (`themes` 653). Searched with three controls -- `nddev_invented_reference_line`, `~/.nddev-not-a-home/skills/` and `NDDEV_INVENTED_SURFACE` -- each zero in the same bytes, so the search discriminates."
      }
    ],
    "declined": [
      {
        "path": ".pi-setup-system",
        "reason": "This provider's own control directory: the target lock, the backup slots and their payloads. Kept out of the declaration for the same reason as the state file, and recorded here because the declined list is where a reader looks before opening a file to find out what it is.",
        "source": "this provider's own contract; no vendor page is involved"
      },
      {
        "path": "AGENTS.override.md",
        "reason": "Pi loads this instead of AGENTS.md or CLAUDE.md from the same directory, so a home holding one ignores the instruction file this provider installs. Not owned, for the reason an override exists at all: it is how a person overrides, and owning it would let `remove` take that away.",
        "source": "https://pi.dev/docs/latest/sdk"
      },
      {
        "path": "NDDEV-PI-PROVIDER.json",
        "reason": "This provider's own state file: which setup is applied, the identity it recorded, and which slot reverses the last operation. Written by every operation and excluded from target identity, because counting it would leave a target different from the identity the operation just wrote. Not a projection surface and never ownable as one.",
        "source": "this provider's own contract; no vendor page is involved"
      },
      {
        "path": "auth.json",
        "reason": "Authentication credentials. Pi joins it against its agent directory -- `agentDir, \"auth.json\"` in the 0.84.3 bundle, beside `keybindings.json`, `models.json` and the owned `settings.json`. Never owned and never captured, for the reason the antigravity list gives: a backup of someone else's credentials is a leak with a schedule. Recorded here because the declaration's `never_touch` is checked against this block.",
        "source": "measured from the 0.84.3 bundle, package/dist/core",
        "note": "**Re-asked at 0.84.4 on 2026-08-31.** The pinned npm package was fetched and its `integrity` checked before a byte was read; the row still stands (`auth.json` 139). Searched with three controls -- `nddev_invented_reference_line`, `~/.nddev-not-a-home/skills/` and `NDDEV_INVENTED_SURFACE` -- each zero in the same bytes, so the search discriminates."
      },
      {
        "path": "git",
        "reason": "Package checkouts the product clones and manages: `~/.pi/agent/git/<host>/<path>` for a global install. Its own shipped docs say what happens to anything a person leaves there -- *\"When reconciliation changes the checkout, pi resets and cleans the clone\"* -- so it is not a surface a setup can promise, and a backup of it would capture somebody else's repository.",
        "source": "measured 2026-08-28 in the product's own shipped documentation, read from the 0.84.3 package at node_modules/@earendil-works/pi-coding-agent/docs",
        "note": "**Re-asked at 0.84.4 on 2026-08-31.** The pinned npm package was fetched and its `integrity` checked before a byte was read; the row still stands (`/git` 2646, `\"git\"` 87). Searched with three controls -- `nddev_invented_reference_line`, `~/.nddev-not-a-home/skills/` and `NDDEV_INVENTED_SURFACE` -- each zero in the same bytes, so the search discriminates."
      },
      {
        "path": "keybindings.json",
        "reason": "A keymap file, joined against the agent directory beside the owned `settings.json`. Not owned, for the reason claude's row gives: no component kind describes a keymap.",
        "source": "measured from the 0.84.3 bundle, package/dist/core",
        "note": "**Re-asked at 0.84.4 on 2026-08-31.** The pinned npm package was fetched and its `integrity` checked before a byte was read; the row still stands (`keybindings.json` 27). Searched with three controls -- `nddev_invented_reference_line`, `~/.nddev-not-a-home/skills/` and `NDDEV_INVENTED_SURFACE` -- each zero in the same bytes, so the search discriminates."
      },
      {
        "path": "managed-config",
        "reason": "Not a path in the target, and recorded because **there is no such path** -- an absence somebody has to measure once so the next reader does not spend the search again. Four of the seven harnesses here carry a system-wide managed policy that overrides everything a user writes; this product carries none.\n\nMeasured 2026-08-29 against the 0.84.4 package, whose bytes match this baseline's own sha256. Searched for the three shapes the other four use -- an `/etc/<product>` literal, a `%ProgramData%\\\\<product>` literal, and a `/Library/Application Support/<product>` literal -- across the whole of `dist/`. Zero hits of any kind. The product's own shipped `docs/settings.md` agrees by omission: it documents exactly two locations, `~/.pi/agent/settings.json` global and `.pi/settings.json` per project, with project overriding global and nothing above either.\n\n**What that means for the `full-auto` posture**: nothing sits above it. On the four harnesses with a managed layer, a permissive posture can install, verify and restore cleanly while an administrator's policy quietly overrides it. Here the keys this provider writes are the last word, which is a stronger statement than it looks and is the reason the absence is worth recording rather than leaving as a gap in the table.\n\nAbsence of a literal is not proof a path cannot exist -- a future release may add one -- so this row says what was searched rather than that none will ever exist.",
        "source": "measured in the 0.84.4 package; docs/settings.md shipped inside it"
      },
      {
        "path": "models-store.json",
        "reason": "A cache of remote model catalogs, persisted so a later run can restore them without a network request, refreshed on a four-hour throttle. A cache the product regenerates is never a configuration surface.",
        "source": "measured 2026-08-28 in the product's own shipped documentation, read from the 0.84.3 package at node_modules/@earendil-works/pi-coding-agent/docs",
        "note": "**Re-asked at 0.84.4 on 2026-08-31.** The pinned npm package was fetched and its `integrity` checked before a byte was read; the row still stands (`models-store.json` 14). Searched with three controls -- `nddev_invented_reference_line`, `~/.nddev-not-a-home/skills/` and `NDDEV_INVENTED_SURFACE` -- each zero in the same bytes, so the search discriminates."
      },
      {
        "path": "models.json",
        "reason": "Named by the consumer's catalog as a second setting surface. The settings documentation does not describe it, and a row nobody can source is not owned.",
        "source": "https://pi.dev/docs/latest/settings"
      },
      {
        "path": "npm",
        "reason": "Where user package installs go, beside the git checkouts above and managed the same way. Declined for the same reason: the product puts content here and takes it away again.",
        "source": "measured 2026-08-28 in the product's own shipped documentation, read from the 0.84.3 package at node_modules/@earendil-works/pi-coding-agent/docs",
        "note": "**Re-asked at 0.84.4 on 2026-08-31.** The pinned npm package was fetched and its `integrity` checked before a byte was read; the row still stands (`npm` 977). Searched with three controls -- `nddev_invented_reference_line`, `~/.nddev-not-a-home/skills/` and `NDDEV_INVENTED_SURFACE` -- each zero in the same bytes, so the search discriminates."
      },
      {
        "path": "pi-debug.log",
        "reason": "Written by the hidden `/debug` command and holding rendered TUI lines with ANSI codes. A log, never owned and never captured.",
        "source": "measured 2026-08-28 in the product's own shipped documentation, read from the 0.84.3 package at node_modules/@earendil-works/pi-coding-agent/docs",
        "note": "**Re-asked at 0.84.4 on 2026-08-31.** The pinned npm package was fetched and its `integrity` checked before a byte was read; the row still stands (`pi-debug.log` 2). Searched with three controls -- `nddev_invented_reference_line`, `~/.nddev-not-a-home/skills/` and `NDDEV_INVENTED_SURFACE` -- each zero in the same bytes, so the search discriminates."
      },
      {
        "path": "trust.json",
        "reason": "A person's saved decisions about which project folders may load project-local settings and resources and **execute project extensions**. The product's own shipped documentation names it five times and is explicit about what turns on it: *\"pi asks before trusting a project folder that contains project-local settings, resources, or project `.agents/skills` and has no saved decision for the folder or a parent folder in `~/.pi/agent/trust.json`. Trusting a project allows pi to load `.pi/settings.json` and `.pi` resources, install missing project packages, and execute project extensions.\"*\n\nNever owned. It is a security decision a person made, not configuration a setup can carry: installing one would grant execution to folders nobody approved, and a restore returning an older copy would silently re-grant a trust that had been withdrawn. Recorded here because it sits inside the home this provider configures and every other file there is accounted for.\n\n**This product has no sandbox of its own**, which is what makes the row matter. Its documentation says so plainly -- *\"Pi does not include a built-in sandbox. Built-in tools can read files, write files, edit files, and run shell commands with the permissions of the pi process\"* -- and points at containers, VMs and micro-VMs instead. So trust is the only gate between a project folder and code running as the person who started the product, and it is a gate this provider must not touch.",
        "source": "the product's own shipped documentation, read from the 0.84.3 package at node_modules/@earendil-works/pi-coding-agent/docs; https://pi.dev/docs/latest/security",
        "note": "**Re-asked at 0.84.4 on 2026-08-31.** The pinned npm package was fetched and its `integrity` checked before a byte was read; the row still stands (`trust.json` 12). Searched with three controls -- `nddev_invented_reference_line`, `~/.nddev-not-a-home/skills/` and `NDDEV_INVENTED_SURFACE` -- each zero in the same bytes, so the search discriminates."
      },
      {
        "path": "working-directories",
        "reason": "One row for `git`, `npm` and `tmp`, joined against the agent directory. Scratch space the product manages for its own operations.",
        "source": "measured from the 0.84.3 bundle, package/dist/core",
        "members": [
          "git",
          "npm",
          "tmp"
        ],
        "note": "**Re-asked at 0.84.4 on 2026-08-31, and the first search said GONE because the needle was this repository's own label.** `working-directories` is a name given here to one row covering three directories; it is not a string the product carries and never was, so searching for it returns zero at every version. The subjects are `git`, `npm` and `tmp`, and in the pinned package's `dist/core` they appear 31, 21 and 1 times. `members` above exists so the next re-ask searches the subject rather than the label. Searched with three controls -- `nddev_invented_reference_line`, `~/.nddev-not-a-home/skills/` and `NDDEV_INVENTED_SURFACE` -- each zero in the same bytes, so the search discriminates."
      },
      {
        "path": "mcp_config.json",
        "reason": "**The vendor says this product has none.** Its own shipped documentation, `usage.md`, under the 0.84.4 bundle's own `package/docs/`, and confirmed against the live page 2026-08-29: *\"It intentionally does not include built-in MCP, sub-agents, permission popups, plan mode, to-dos, or background bash. You can build or install those workflows as extensions or packages…\"*\n\nSo there is no MCP surface at any scope, and the capability arrives through `extensions/`, which this provider owns and routes as `plugin`. A stated absence is worth more than a missing file: it says the next release will not quietly add one under a name nobody guessed.",
        "source": "the product's own shipped documentation, usage.md under the pinned bundle's package/docs/; https://pi.dev/docs/latest/usage"
      },
      {
        "path": "agents",
        "reason": "No sub-agents, from the same sentence as the MCP row above: *\"It intentionally does not include built-in MCP, **sub-agents**, permission popups…\"* The `agent` kind is therefore not declared for this harness, and nothing under this home is read as one.",
        "source": "the product's own shipped documentation, usage.md under the pinned bundle's package/docs/; https://pi.dev/docs/latest/usage"
      },
      {
        "path": "hooks.json",
        "reason": "Hooks here are an **extension API concept, not a configuration surface**. `extensions.md`, under the pinned bundle's own `package/docs/`, documents `session_start`, a `spawnHook` around tool execution and session-scoped teardown hooks -- all of them functions inside an extension module. There is no `hooks.json` and no `hooks` key in `settings.json`, so a hook reaches this product through `extensions/`, which is owned and routes `plugin`.",
        "source": "the product's own shipped documentation, extensions.md under the pinned bundle's package/docs/"
      }
    ],
    "config_home_env": "PI_CODING_AGENT_DIR",
    "config_home_env_note": "Documented at https://pi.dev/docs/latest/settings and recorded in this baseline as `configuration.environment_override`. Not measured by writing: no credential-free command makes this product write its home.",
    "configuration_format": {
      "file": "settings.json",
      "grammar": "json",
      "accepts_comments": false,
      "note": "JSON. The vendor documents no comment support and publishes no schema; searched 2026-08-28 and none found on SchemaStore or the vendor's own site.",
      "measured_at": "2026-08-28"
    },
    "scoped": [
      {
        "target_scope": "user_root",
        "root": "~/.agents",
        "surfaces": [
          {
            "path": "skills",
            "kinds": [
              "skill"
            ],
            "shape": "directory",
            "source": "measured from the pinned bundle, digest verified before reading (pi 0.84.4, package/dist/core/package-manager.js)",
            "evidence": "bytes",
            "note": "`package-manager.js:1976` builds `userAgentsSkillsDir = join(getHomeDir(), \".agents\", \"skills\")` and line 2017 loads from it: `addResources(\"skills\", collectAutoSkillEntries(userAgentsSkillsDir, \"agents\"), ...)`. No Pi page says so. A neighbouring use in `trust-manager.js:160` *excludes* this directory while walking up for a project-scoped one, which is what a first reading of the variable name would have mistaken for the read -- the line that matters is 2017, not 1976.\n\n**`bytes` and not `ran`, and what was tried, so the next reader does not repeat it.** The pinned `0.84.4` bundle was installed into a temporary prefix and every credential-free entry point exercised: `pi list` reports installed *packages* and an auto-loaded skill is a resource rather than a package, `pi config` is a TUI, and `--verbose --offline --print` reaches provider selection and exits before any skill is resolved. There is no command that reports the resolved skill set without a credential. Codex, opencode and grok each have one; this product does not, and the absence is a property of the product rather than of the effort.\n\nThe one root in this estate that belongs to a convention rather than to a product. `$HOME/.agents/skills` is a *sibling* of this product's configuration home, not a child, so nothing declared against this provider's own target can reach it -- that is what `user_root` exists for.\n\n**Owning a shared root, and the reason this record used to decline it.** Five of the seven products read this root, and the decline said: *a namespace is removed whole, so a second declaration would make either provider's remove take the other's skills.* That sentence was true when it was written and stopped being true when `written_paths` shipped -- `remove` under this scope takes the files this provider recorded writing and refuses rather than widening when it cannot read the record, and each harness carries its own state file, so they coexist under one root. The reason was not re-read when the thing it described changed.\n\nRelative to this scope's own root the path is `skills`, not `.agents/skills`: the root is what the scope names, and writing it into the path again would put the skills at `~/.agents/.agents/skills`."
          }
        ],
        "declined": []
      }
    ],
    "projection_basis": {
      "package": "extensions"
    },
    "configuration_schema_note": "**One schema is published and it is not for the file we write.** The pinned package carries `https://raw.githubusercontent.com/earendil-works/pi/main/packages/coding-agent/src/modes/interactive/theme/theme-schema.json`, five times -- a **theme** schema. `settings.json` names none and there is none to name. This estate ships no theme in any of pi's four postures, so the schema has no subject here; it is recorded so a later posture that does ship one knows the schema exists. Surveyed 2026-08-31 across all seven; three publish one and use it, cursor publishes one it must not name, and these three publish none."
  },
  "software_artifacts": {
    "command": "pi",
    "shape": "gzip-tar",
    "platforms": {
      "linux/arm64": {
        "url": "https://github.com/earendil-works/pi/releases/download/v0.85.0/pi-linux-arm64.tar.gz",
        "bytes": 42774976,
        "sha256": "sha256:821750e0ac6bf6e10c35b93ddab88a44f2d0ef8411af9ea4e8ffe620f62130df",
        "member": "pi/pi"
      },
      "linux/x86_64": {
        "url": "https://github.com/earendil-works/pi/releases/download/v0.85.0/pi-linux-x64.tar.gz",
        "bytes": 42708859,
        "sha256": "sha256:a7e7c65f1dc528d2e17e7d946ad2b61df0e2b0f9952faee77807c2484b464d6e",
        "member": "pi/pi"
      },
      "macos/arm64": {
        "url": "https://github.com/earendil-works/pi/releases/download/v0.85.0/pi-darwin-arm64.tar.gz",
        "bytes": 31183100,
        "sha256": "sha256:b0a1a3ab9708047e31b76a27911e8b445b3e4a38e2f46a08b6635df75f3499c0",
        "member": "pi/pi"
      },
      "macos/x86_64": {
        "url": "https://github.com/earendil-works/pi/releases/download/v0.85.0/pi-darwin-x64.tar.gz",
        "bytes": 33687389,
        "sha256": "sha256:611290e032a47f1546bd30e12c14a59a600a24662d5239c0c159ef3c7a0ca3b0",
        "member": "pi/pi"
      },
      "windows/arm64": {
        "url": "https://github.com/earendil-works/pi/releases/download/v0.85.0/pi-windows-arm64.zip",
        "bytes": 43703991,
        "sha256": "sha256:c10fb6f30f188b1eba61608e0d33453456ee9805ff543fdd99bcaf85f2d949df",
        "shape": "zip",
        "member": "pi.exe"
      },
      "windows/x86_64": {
        "url": "https://github.com/earendil-works/pi/releases/download/v0.85.0/pi-windows-x64.zip",
        "bytes": 45158082,
        "sha256": "sha256:526085e0206acb8e8f9997efcd4e3654fb8a47a04318e09e7324ed5abe549586",
        "shape": "zip",
        "member": "pi.exe"
      }
    },
    "note": "**The vendor's standalone release, not its npm package, since 2026-08-31.** The npm tarball is 6.8MB of JavaScript whose entry point is `dist/bundle/cli.js` with a `#!/usr/bin/env node` shebang. Installing it left a host without Node >= 22.19 with a program that installed and could not start -- an install that succeeds and a launch that fails, with nothing between them saying so.\n\nThe release publishes one standalone archive per platform for all six declared hosts, and each was fetched, checked against the vendor's own `SHA256SUMS`, and recorded from the bytes. **Two layouts in one release:** the four gzip-tars carry the executable at `pi/pi`, and the two Windows ZIPs carry `pi.exe` at the root. A rule built from the platform name would have been right four times -- the same lesson cursor and antigravity each taught once, on the container rather than the path.\n\n**Measured, with the absence controlled:** the whole lifecycle -- install, launch, update from 0.84.3, rollback, roll forward, inactive removal, removal -- runs with `node` unreachable on `PATH`, and the run checked that it was unreachable. The product answers `0.84.4` from the exposed command.\n\nthe pin refresher reads the release rather than the registry now, and refuses an artifact whose bytes disagree with the vendor's published digest. The twelve records here were produced twice -- once by hand and once by the tool -- and compared.",
    "version": "0.85.0",
    "verified_at": "2026-09-04T13:31:46+00:00"
  },
  "setup_catalogue_digest": "sha256:51de6d293415c5babb6cf29c6c56f92cc50e24ed69f40a0938cb16267a71cbd5",
  "previous_software_artifacts": {
    "command": "pi",
    "shape": "gzip-tar",
    "platforms": {
      "linux/arm64": {
        "url": "https://github.com/earendil-works/pi/releases/download/v0.84.4/pi-linux-arm64.tar.gz",
        "bytes": 42529658,
        "sha256": "sha256:135580f6b942151646e67b8b866d987d28ce3cff5a497030775ddd29659f943d",
        "member": "pi/pi"
      },
      "linux/x86_64": {
        "url": "https://github.com/earendil-works/pi/releases/download/v0.84.4/pi-linux-x64.tar.gz",
        "bytes": 42464648,
        "sha256": "sha256:c2f3c3e6a1850bd87654cc3ca8811013272397c3d042a4e2a64c43ee1b423972",
        "member": "pi/pi"
      },
      "macos/arm64": {
        "url": "https://github.com/earendil-works/pi/releases/download/v0.84.4/pi-darwin-arm64.tar.gz",
        "bytes": 30928407,
        "sha256": "sha256:c68e3ac4d05b4e282aaab2e6c76f161d3e9e68f19a22e38913cbfaadb6c800f0",
        "member": "pi/pi"
      },
      "macos/x86_64": {
        "url": "https://github.com/earendil-works/pi/releases/download/v0.84.4/pi-darwin-x64.tar.gz",
        "bytes": 33440191,
        "sha256": "sha256:7a042d6413065421387001a4986190a1a03186c95a695f4dee0bdc76e60de8f7",
        "member": "pi/pi"
      },
      "windows/arm64": {
        "url": "https://github.com/earendil-works/pi/releases/download/v0.84.4/pi-windows-arm64.zip",
        "bytes": 43455125,
        "sha256": "sha256:6b2726efc34a9158ab06bf7b981f7bcccf15de9ea236a3f4ef7a894a78aa386e",
        "shape": "zip",
        "member": "pi.exe"
      },
      "windows/x86_64": {
        "url": "https://github.com/earendil-works/pi/releases/download/v0.84.4/pi-windows-x64.zip",
        "bytes": 44907374,
        "sha256": "sha256:03b2318774f18721e959d9f8f3340a9f942e7aa516fb7030d3007a12a40a4a97",
        "shape": "zip",
        "member": "pi.exe"
      }
    },
    "note": "**The vendor's standalone release, not its npm package, since 2026-08-31.** The npm tarball is 6.8MB of JavaScript whose entry point is `dist/bundle/cli.js` with a `#!/usr/bin/env node` shebang. Installing it left a host without Node >= 22.19 with a program that installed and could not start -- an install that succeeds and a launch that fails, with nothing between them saying so.\n\nThe release publishes one standalone archive per platform for all six declared hosts, and each was fetched, checked against the vendor's own `SHA256SUMS`, and recorded from the bytes. **Two layouts in one release:** the four gzip-tars carry the executable at `pi/pi`, and the two Windows ZIPs carry `pi.exe` at the root. A rule built from the platform name would have been right four times -- the same lesson cursor and antigravity each taught once, on the container rather than the path.\n\n**Measured, with the absence controlled:** the whole lifecycle -- install, launch, update from 0.84.3, rollback, roll forward, inactive removal, removal -- runs with `node` unreachable on `PATH`, and the run checked that it was unreachable. The product answers `0.84.4` from the exposed command.\n\nthe pin refresher reads the release rather than the registry now, and refuses an artifact whose bytes disagree with the vendor's published digest. The twelve records here were produced twice -- once by hand and once by the tool -- and compared.",
    "version": "0.84.4",
    "verified_at": "2026-09-03T14:08:27+00:00"
  },
  "surface_presence": {
    "measured_at": "2026-09-02",
    "artifact": "https://github.com/earendil-works/pi/releases/download/v0.84.4/pi-linux-x64.tar.gz",
    "artifact_sha256": "sha256:c2f3c3e6a1850bd87654cc3ca8811013272397c3d042a4e2a64c43ee1b423972",
    "product_version": "0.84.4",
    "declared_surfaces": 9,
    "present": 9,
    "absent": [],
    "control": "zzq-invented-surface-nobody-ships",
    "control_hits": 0,
    "method": "Every namespace this harness declares, global and scoped, searched as a fixed string in the artifact the current pin names, digest verified before reading; both the declared path and its last segment counted, because a product that composes a path by joining segments never spells the whole of it. A count here is a lower bound and not a routing proof -- what it answers is whether a surface vanished from the product. This pin did not move on 2026-09-02 when five others did; the sweep covers all seven so the next bump has something to differ from. The invented control was searched in the same run and found nowhere, so the search discriminates."
  }
}
