# Changelog

This file is a release ledger: every heading below is a real release.

The project follows Semantic Versioning.

## [Unreleased]

- Initial public tree for `pi-setup-system`: the shared lock, journal,
  backup and atomic-apply kernel, the verified ai-stp provider kit v3, the
  carried runtime baseline, and the full public OSS workflow suite on Linux,
  macOS and Windows.
